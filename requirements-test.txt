pytest
# Nécessaire pour importer vision.db.chat_db (test_chat_db.py) — pas d'autre
# dépendance lourde : vision_config.py et vision.ai.emotion_parser sont
# volontairement stdlib-only pour rester testables sans conf/secret ni
# openai/opencv/pyaudio installés.
sqlobject
# Nécessaire pour tester vision.audio.effects (trémolo/distorsion) et
# vision.audio.beeps (bips de réflexion) — pas d'autre dépendance audio en CI
# (openai/pyaudio/scipy/piper/faster_whisper restent hors requirements-test.txt).
# <2 : mediapipe (V1, hors CI) impose déjà numpy<2 en prod, on aligne la CI sur
# la même contrainte pour ne pas tester un comportement numpy que la prod n'a pas.
numpy<2
