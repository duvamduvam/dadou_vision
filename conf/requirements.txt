# Requirements MINIMAL — chaque ligne justifiée (V0 socle sain, cf. ARCHITECTURE.md).
# Rien d'autre ne doit être ajouté sans raison documentée ici.

# Client officiel OpenAI : GPT-4o (vision/ai/interactions.py) et TTS
# streaming (vision/ai/tts.py). Version récente pour l'API Chat Completions
# et le client audio streaming actuels.
openai>=1.50.0

# Historique de conversation persistant en SQLite (vision/db/chat_db.py)
sqlobject

# Lecture de la réponse TTS sur la sortie audio ALSA (vision/ai/tts.py,
# PyAudio.open + player_stream.write)
pyaudio~=0.2.14

# Capture caméra USB (vision/ai/camera.py). Variante headless : pas de
# dépendances GUI/Qt superflues sur un Pi 5 sans écran.
opencv-python-headless

# Traitement du signal pour l'effet de voix robotique (resample + tremolo),
# vision/ai/tts.py — gardé tel quel de la version historique.
numpy
scipy
